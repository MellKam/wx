(module
  (func $fibonacci (export "fibonacci") (param $n_0 i32) (result i32)
    (local $a_1 i32)
    (nop)
    (if (result i32)
      (local.get $a_1)
      (then
        (i32.const 1))
      (else
        (i32.const 0)))))
