module test;

fn get_tuple() -> (i32, (Point, i32)) {
  (1, (Point::{ x: 2 }, 3))
}

fn main() -> Point {
  Point::{
    x: g: {
      break :g 5;
    },
  }
}

pub enum Color: i32 {
  Red = 1,
  Green,
  Blue,
}

pub struct Point {
  x: i32,
}

module s {
  const PI = 3.14;
}

const PI: i32 = 3;

trait Add {
  fn add(lhs: Self, rhs: Self) -> Self;
}

impl Add for i32 {
  fn add(lhs: i32, rhs: i32) -> i32 {
    lhs + rhs
  }
}

export {
  main,
}
